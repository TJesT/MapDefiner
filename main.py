from os.path import isfile, join
from os import listdir
import MapImageParser as MIP

maps = "maps/"
list()

#data = {
#    fn: cv2.imread(join(maps, fn)) for fn in listdir(maps) if isfile(fn)
#}

 mip = MIP()